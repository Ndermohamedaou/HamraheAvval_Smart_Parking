class AlarmHandler {
  final alarmType;
  AlarmHandler(this.alarmType);

  // showAlarm({String status, String title, String message}) {
  //   switch (alarmType) {
  //     case 'flush':
  //       showSnackBar(status, title, message);
  //       break;
  //     case 'dialog':
  //       showDialog(status, title, message);
  //       break;
  //     default:
  //     // showSnackBar(status, title, message);
  //   }
  // }
}
